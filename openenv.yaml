tasks:
  - name: easy
    description: Basic intent classification
    metric:
      type: python
      path: easy.py:grader

  - name: medium
    description: Intent + priority classification
    metric:
      type: python
      path: medium.py:grader

  - name: hard
    description: Full response evaluation
    metric:
      type: python
      path: hard.py:grader